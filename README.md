# 🧠 TalkBoard — AppService

The **AppService** is the backend core of the TalkBoard ecosystem.  
It handles authentication sync, user data management, database operations, and
acts as the primary application API service.

> Tech Stack: **Node.js + Express + Prisma + PostgreSQL (Supabase) + TypeScript**

---

## 🚀 Features (Current)

- Connects to PostgreSQL database
- Prisma ORM integrated
- Supabase OAuth integration
- Syncs authenticated user data to DB
- Structured backend architecture (routes → controllers → services → db)

---
